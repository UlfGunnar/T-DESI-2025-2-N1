1 - Iria fazer todas as combinações possíveis, produto cartesiano praticamente.

2 - 

select 
	c.first_name, 
	c.last_name,
	a.address  
from customer as c
join address as a on c.address_id = a.address_id;


3 -

select 
	f.title,
	c.name
from film as f
join film_category as fc on f.film_id = fc.film_id 
join category as c on fc.category_id = c.category_id;

4 -

select
	*
from customer 
join payment  using(customer_id);

5 - se não for feito pela chave, fara junções indesejáveis

6 - 

select 
	*
from rental
where return_date  is null;

7 - 

select 
	rental_id
from rental
where return_date  is not null;

8 - Por que o NULL não pode ser usado em operações lógicas, pois é um valor desconhecido

9 - 

select 
	*
from film as f
left join inventory as i on f.film_id = i.film_id
where i.inventory_id is not null;

10 - 1 linha, como o where pede para só pegar valores NULL, só vai pegar o NULL

11 - 